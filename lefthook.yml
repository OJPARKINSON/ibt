assert_lefthook_installed: true
skip_lfs: true

pre-commit:
  parallel: true
  jobs:
    - name: lint & test
      glob: "*.go"
      group:
        jobs:
          - run: go lint .
            tags: lint
            stage_fixed: true

          - run: go test .
            tags: test
